# Server-launch config: trained frame detector loaded from the HuggingFace Hub
# (EarthSpeciesProject/sed-birdcode-ablation-audioprotopnet_nofinetune).
# Serve with: SED_MODEL_CONFIG=this uv run sed-server

type: frame
hf_repo_id: EarthSpeciesProject/sed-birdcode-ablation-audioprotopnet_nofinetune
